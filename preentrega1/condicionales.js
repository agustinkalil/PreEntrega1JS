
const inicioDeSesion = () => {
    let correo = prompt(`Ingrese su correo para iniciar sesion`);
let contraseña = prompt(`Ingresar contraseña`);

    if(correo === `correo@correo.com` && contraseña === `Blackrose`){
        alert(`Bienvendido !`);
    } else if(
        alert(`usuario incorrecto`)
    );
}

inicioDeSesion();